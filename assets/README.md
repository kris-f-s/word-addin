# 图标文件目录

此目录用于存放插件的图标文件。

## 需要的文件

- `icon-16.png` - 16x16 像素
- `icon-32.png` - 32x32 像素
- `icon-64.png` - 64x64 像素
- `icon-80.png` - 80x80 像素

## 创建图标

请参考项目根目录下的 `create-icons.md` 文件了解如何创建这些图标文件。

如果这些文件不存在，Word 会使用默认图标，插件仍然可以正常工作。

